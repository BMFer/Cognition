# tests/

Executable tests for everything in `src/` and for the protocol scaffolding itself (e.g., the artifact validator). PRs that change `src/` or `scripts/` must include test coverage; PRs that change the protocol must include tests for the new validation rules.

Tests are required evidence, not decoration. The PR's `## Test Results` section cites runs from this directory.

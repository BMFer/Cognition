# Cognition CODEOWNERS
#
# These owners are auto-requested for review on every PR. The protocol requires
# that the opposite cluster review every PR before the governor merges, so both
# cluster identities are registered here.
#
# Until each cluster has a real GitHub account, the wildcard-route line at the
# bottom remains commented. Filling in real GitHub handles, plus enabling
# "Require review from Code Owners" in branch protection (see
# docs/branch-protection.md), is what makes the cross-cluster review rule
# binding. Until then, the file is documentation, not enforcement.

# -----------------------------------------------------------------------------
# Claude Cluster
#   Identity:       Claude (Opus 4.7), Anthropic's CLI assistant
#   Role:           primary builder, architect, refactor proposer
#   Authorship:     commits land with the trailer
#                   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
#   PR attribution: PR body declares "**Cluster**: claude"
#                   (validator enforces this on every PR)
#   GitHub handle:  <PLACEHOLDER - governor created the backing Gmail account
#                    jerrame.hertz.claude.cluster@gmail.com on 2026-05-08;
#                    planned GitHub username is "claude-cluster". Replace
#                    this line and update the wildcard rule below from
#                    claude-cluster-bot to claude-cluster once the GitHub
#                    account is created and accepts the repo collaborator
#                    invite.>
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Codex Cluster
#   Identity:       Codex (OpenAI), coding agent inside the Codex desktop app
#   Role:           adversarial reviewer, test author, alternate implementation proposer
#   Authorship:     commits land with an explicit Codex trailer when Codex authors
#                   the change:
#                   Co-Authored-By: Codex <codex@openai.com>
#   Review stance:  prioritize defects, regressions, missing tests, and protocol
#                   violations before summaries or implementation praise
#   PR attribution: PR body declares "**Cluster**: codex"
#                   (validator enforces this on every PR)
#   GitHub handle:  @codex-cluster-bot (registered 2026-05-08 by governor,
#                   backed by Gmail subaddress
#                   jerrame.hertz+codex-cluster@gmail.com; repo
#                   collaborator invite + signing key registration: TBD)
# -----------------------------------------------------------------------------

# Once both GitHub handles exist, uncomment the line below to activate
# auto-requested cross-cluster review on every PR:
#
*  @claude-cluster-bot  @codex-cluster-bot

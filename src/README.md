# src/

Source code for cognition tooling: cluster runners, artifact extractors, conflict detectors, and any other code that participates in the protocol itself.

Code that exists only to support a single experiment belongs elsewhere or in a feature branch, not here.
